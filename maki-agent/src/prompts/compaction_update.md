The summary in <previous-summary> tags covers the conversation before the messages above. Update it so it covers both, and write the result out in full.

Rules:
- PRESERVE everything in the previous summary that is still true, even if the messages above do not mention it
- ADD what the messages above contributed
- UPDATE status: move finished work out of in-progress, refresh the todo list, drop blockers that are resolved
- PRESERVE file paths, function names, commands, and error messages exactly as written
- REMOVE something only when the messages above show it is obsolete or wrong
